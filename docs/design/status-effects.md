# ProjectS 状態異常・継続ダメージ設計

## 基本方針

属性は、直撃ダメージ、弱点特効、属性固有の状態異常・戦闘性質をまとめて作るために使用する。

火・氷・雷の各攻撃は属性値を持つことができ、属性値は通常攻撃・スキルの直撃にも反映される。

ただし、属性による追加のダメージ増幅が発生するのは、PvEでモブ・ボスの弱点属性を突いた時のみとする。

状態異常・継続ダメージは、火・氷・雷それぞれのプレイ感を差別化する中核システムとして扱う。

## 火属性

### 決定済みの基本構造

火属性攻撃は、対象へ燃焼を付与しながら炎蓄積を増加させる。

炎蓄積が一定量に達すると、爆燃が発生して瞬間ダメージを与える。

役割は以下のように分ける。

- 火属性直撃: 通常攻撃・スキルを当てた瞬間の火属性ダメージ
- 燃焼: 継続的に敵へ圧力を与える
- 爆燃: 攻撃を重ねて蓄積した結果として瞬間火力を発生させる

基本的な戦闘ループ:

1. 火属性攻撃を当て、火属性直撃ダメージを与える
2. 燃焼を付与・維持する
3. 炎蓄積を増やす
4. 閾値に達すると爆燃が発生する
5. 再び炎蓄積を溜める

これにより火属性は、直撃、継続圧力、周期的な爆発力を持つ属性になる。

### 火属性値

火属性攻撃は、その攻撃が持つ火の強さを表す「火属性値」を持つ。

火属性値は主に以下の計算基準として使用する。

- 通常攻撃・スキルの火属性直撃ダメージ
- 燃焼ダメージ
- 一回の攻撃で加算する炎蓄積量
- 爆燃ダメージ
- PvEで火弱点を突いた時の追加特効

例:

- 基礎直撃ダメージ: 100
- 火属性値: 40

この攻撃は、基礎直撃100に加えて、火属性値40を基準とした火属性直撃ダメージを与える。

火属性直撃が火属性値の何%をダメージとして使用するかは、通常攻撃・スキルごとの属性反映率で調整する。

火属性値40が必ずそのまま40ダメージになるとは限らない。

例:

- 火属性値: 40
- 属性反映率: 50%
- 火属性直撃部分: 20

最終的な火属性直撃部分も、元の攻撃が物理系統なら物理耐久、魔法系統なら魔法耐久で計算する。

### 火属性値を付与・強化するMOD

火属性を持たない装備・攻撃へ火属性を追加する固定値MOD:

- `火属性値 +x`

すでに持っている火属性値を割合で伸ばすMOD:

- 正式名称: `火属性値 +%`
- ゲーム内短縮表示候補: `火属性 +%`

例:

- 元の火属性値: 40
- 火属性値 +50%
- 最終火属性値: 60

最終火属性値60を基準として、火属性直撃、燃焼、炎蓄積、爆燃、火弱点特効を計算する。

`火属性ダメージ +%` という名称は採用しない。直撃だけでなく燃焼・蓄積・爆燃・弱点特効まで伸ばすため、`火属性値 +%` の方が仕様を正確に表せる。

### 火属性ビルド内の役割分担

火属性値 +%:

- 火属性値全体を広く伸ばす
- 火属性直撃、燃焼、炎蓄積、爆燃、火弱点特効を平均的に強化する
- 火属性ビルドを成立させる基礎MOD

燃焼専用MOD:

- 燃焼ダメージ
- 燃焼持続時間
- 燃焼中の対象への追加効果

など、燃焼だけを高効率で伸ばす専門MODとして扱う方向とする。

爆燃専用MOD:

- 炎蓄積量
- 爆燃ダメージ
- 爆燃範囲
- 爆燃発生後の再蓄積補助

など、爆燃だけを高効率で伸ばす専門MODとして扱う方向とする。

直撃型では、火属性値 +%に加えて、攻撃力、通常攻撃・スキルダメージ、近接・投射物・魔法ダメージ、クリティカル、貫通などを組み合わせる。

これにより、同じ火属性でも以下のビルドを成立させる。

- 火属性汎用型: 直撃、燃焼、爆燃を平均的に扱う
- 火属性直撃型: 通常攻撃・スキルの瞬間火力を重視する
- 燃焼特化型: 長時間の継続圧力を重視する
- 爆燃特化型: 蓄積から発生する周期的な瞬間火力を重視する

専用MODは対象が限定される代わりに、火属性値 +%より数値効率を高くする方向とする。

### 攻撃系統との関係

火属性直撃、燃焼、爆燃は、元となった攻撃の攻撃系統に従う方向とする。

- 物理系統の火属性攻撃から発生した効果は物理耐久を参照する
- 魔法系統の火属性攻撃から発生した効果は魔法耐久を参照する

属性が火であることを理由に、属性耐性や別系統の防御値は参照しない。

### 弱点との関係

火属性値は通常の敵への直撃・燃焼・爆燃にも有効になる。

火弱点を持つモブ・ボスに攻撃した場合は、それらに加えて弱点特効による追加増幅が発生する。

つまり以下の関係になる。

- 通常の敵: 火属性値による直撃・燃焼・爆燃が発生する
- 火弱点の敵: 通常効果に加えて弱点特効が発生する
- PvP: 属性弱点特効は発生しないが、直撃・燃焼・爆燃は発生する

### 未決定事項

以下は今後一つずつ決める。

- 通常攻撃・各スキルの火属性反映率
- 燃焼の具体的なダメージ計算方法
- 燃焼の持続時間
- 燃焼の重複・更新・上書きルール
- 火属性値から炎蓄積量へ変換する具体的な比率
- 爆燃の具体的なダメージ計算方法
- 爆燃後に炎蓄積を完全リセットするか
- 同じ対象で爆燃するたび必要蓄積量を増やすか
- 手数武器と重い一撃の蓄積バランス
- ボスへの燃焼・爆燃の効き方
- PvPでの燃焼時間、蓄積減少、爆燃ダメージ調整
- 燃焼・爆燃専用MODの正式名称、数値幅、出現部位

## 氷属性

減速、凍結、行動制御を中心とする方向。火属性と同様に、氷属性値は直撃と氷固有効果の基準にする。詳細は未決定。

## 雷属性

瞬間火力、連鎖、連続ヒット、高速性を中心とする方向。火属性と同様に、雷属性値は直撃と雷固有効果の基準にする。詳細は未決定。
