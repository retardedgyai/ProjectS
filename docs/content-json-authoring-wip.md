# Experimental JSON content authoring

This branch preserves experimental `content.definition` and
`content.persistence` JSON authoring and persistence work for future review.
It is not runtime-wired. Existing Mob Editor YAML remains canonical; this
branch does not convert or replace that format.
