package io.github.gyai.projects.combat.element;

/** Pure target category; a later adapter may map mob definitions into it. */
public enum ElementTargetCategory {
    NORMAL,
    ELITE,
    MINIBOSS,
    BOSS
}
